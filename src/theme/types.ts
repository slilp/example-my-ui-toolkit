export type Breakpoints = string[];

export type MediaQueries = {
  xs: string;
  sm: string;
  md: string;
  lg: string;
  xl: string;
};

export type Colors = {
  primary: string;
  secondary: string;
  success: string;
  error: string;
  warning: string;
};

export type Variants =
  | 'primary'
  | 'secondary'
  | 'success'
  | 'error'
  | 'warning';
